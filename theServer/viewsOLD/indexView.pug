extends layout

block metas

block title
  title ThisGreatApp!

block headScripts

block bodyScripts

block content

  section(id="header", class="header-section current")
    div(class="container")
     div(class="row")

  section(id="login-signup", class="login-signup-section")
    div(class="container")
     div(class="row")

  section(id="about", class="about-section")
    div(class="container")
     div(class="row")

  section(id="team", class="team-section")
    div(class="container")
     div(class="row")

  section(id="contact", class="about-section")
    div(class="container")
     div(class="row")

  section(id="members-only", class="members-only-section")
    div(class="container")
     div(class="row")
